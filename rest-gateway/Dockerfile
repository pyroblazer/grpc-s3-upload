FROM golang:1.22-alpine AS build;WORKDIR /app;COPY . .;RUN go mod init rest-gateway && go mod tidy && go build -o gateway;FROM alpine:3.19;WORKDIR /app;COPY --from=build /app/gateway .;CMD ["./gateway"]
